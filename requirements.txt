requests>=2.31
Flask>=3.0

# Required for Doodle group polls (and the Calendly / Rally browser adapters):
# the Doodle SPA needs a real browser to authenticate before its API is readable.
#   pip install playwright && playwright install chromium
playwright>=1.44
